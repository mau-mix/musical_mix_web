import styled from "styled-components";

export const IconWatss = styled.a`
background-color: ${({ theme }) => theme.font.colors.primary};
   margin: 0px 1rem;
  &:hover {
      cursor: pointer;
      color: white;
    }
`